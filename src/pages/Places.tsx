// Root / entry point for Places feature

import { PlacesList } from "../components/Places";

const PlacesPage = () => {
  return (
    // layout stuff, places routes
    <PlacesList />
  );
};

export default PlacesPage;
